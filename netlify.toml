[build]
  command = "cd client && npm install && npm run build"
  publish = "client/.next"

[functions]
  directory = "server"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "20"
